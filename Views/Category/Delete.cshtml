@model AimsCarRentals.Models.ViewModel.CategoryViewModel
<style>
    .container {
        padding: 0 16px;
    }

        .container::after, .row::after {
            content: "";
            clear: both;
            display: table;
        }
</style>
<div clasid's="container">
    <div>
        Are you sure you want to delete category @Model.Name
        <div class="container">
            <a asp-action="Index" class="btn btn-dark">Cancel</a><br />&nbsp;
            <a asp-action="Delete" class="btn btn-danger">Delete</a><br />
        </div>
    </div>
</div>