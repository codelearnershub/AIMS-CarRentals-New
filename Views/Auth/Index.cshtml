@model IEnumerable<AimsCarRentals.Models.ViewModel.UserViewModel>
@{
    ViewData["Title"] = "Category";
    int id = 1;
}

<style>
    table {
        border-collapse: collapse;
        border-spacing: 0;
        width: 100%;
        border: 1px solid #ddd;
    }

    th, td {
        text-align: left;
        padding: 16px;
    }

    tr:nth-child(even) {
        background-color: #f2f2f2
    }
</style>

<<<<<<< HEAD:Views/Auth/Index.cshtml

<h2> Users</h2>
=======
<a asp-controller="Auth" asp-action="Index" class="btn btn-danger">Register Customer</a>
<h2>Branch Table</h2>
>>>>>>> origin/master:Views/User/Index.cshtml
<table>
    <tr>
        <th>Id</th>
        <th>First Name</th>
        <th>Middle Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Actions</th>
    </tr>
    @foreach (var user in Model)
    {
<tr>
    <td>@(id++)</td>
    <td>@user.FirstName</td>
    <td>@user.MiddleName</td>
    <td>@user.LastName</td>
    <td>@user.Email</td>
    <td>
        <a asp-controller="Bookings" asp-action="Details" class="btn btn-danger" asp-route-Id="@user.Id">Details</a>
        <a asp-controller="Bookings" asp-action="Edit" class="btn btn-danger" asp-route-Id="@user.Id">Edit</a>
        <a asp-controller="Bookings" asp-action="Delete" class="btn btn-danger" asp-route-Id="@user.Id">Delete</a>
    </td>
</tr>
    }
</table>

